services:

#backend container
  backend-container:
    build:
      context: ./backend  
      dockerfile: Dockerfile
    container_name: backend
    image: fortune-cookie-backend
    ports:
      - "9000:9000"

  frontend-container:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    image: fortune-cookie-frontend
    ports:
      - "8080:8080"
